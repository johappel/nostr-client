{
  "name": "relay-test-runner",
  "version": "1.0.0",
  "description": "Real Nostr Relay Tests with live data",
  "scripts": {
    "test:relay": "cd tests && npm run dev",
    "build:all": "cd framework && npm run build && cd ../tests && npm run build",
    "setup": "npm install && cd framework && npm install && cd ../tests && npm install",
    "start:windows": "start-relay-tests.bat",
    "start:unix": "./start-relay-tests.sh"
  },
  "keywords": ["nostr", "relay", "testing", "real-data"],
  "dependencies": {
    "nostr-tools": "^2.8.1"
  }
}